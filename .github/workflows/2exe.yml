name: Zad2 - scheduled job

on:
 schedule:
   - cron: "0 */6 * * 1-5"
 workflow_dispatch:
   
jobs:
  display-current-date:

    runs-on: ubuntu-latest

    steps:
    - name: Print current date
      run: |
        echo "Current date:"
        echo $(date +'%Y-%m-%dT%H-%M-%S')
